//? call: Sets this and immediately executes the function with arguments passed individually.

//?apply: Sets this and immediately executes the function with arguments passed as an array.

//?bind: Sets this and creates a new function with optional preset arguments, which can be called later.